#!/ usr/bin/node
const linesPrinted = [
    "C is fun",
    "Python is cool",
    "JavaScript is amazing"
];

for (let i = 0; i < linesPrinted.length; i++) {
    console.log(linesPrinted[i]);
}

/* for (const line of lines) {
  console.log(line);
}*/ //this is a while loop version